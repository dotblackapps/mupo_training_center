@extends(theme('auth.layouts.app'))
@section('content')
<div class="mupo-auth-page"><div class="mupo-auth-shell"><aside class="mupo-auth-side"><div><a href="{ url('/') }" class="mupo-auth-home"><i class="fa-solid fa-arrow-left"></i> Back to Home</a><a href="{ url('/') }" class="mupo-auth-logo"><img src="{ asset('mupo/assets/images/mupo-logo_1.jpeg') }" onerror="this.src='{ url('public/uploads/settings/mupo-logo_1.jpeg') }'" alt="Mupo Training Center"></a><h1>Protecting your account.</h1><p>Complete the verification step to continue securely.</p><div class="mupo-auth-points"><div class="mupo-auth-point"><b>Secure access</b><br>Your account information is protected.</div><div class="mupo-auth-point"><b>Learner support</b><br>Contact MUPO support when you need assistance.</div></div></div><p><b>Mupo Training Center</b><br>Think. Future. Forward.</p></aside><main class="mupo-auth-main"><section class="mupo-auth-card"><span class="mupo-auth-badge">Security Check</span><h2>{{ __('frontend.2-step Verification') }}</h2><p class="lead">{{ __('frontend.We sent a verification code to your email. Enter the code from the email in the field below.') }}</p><form action="{{ route('login') }}" method="POST" id="loginForm">@csrf<input type="hidden" name="email" value="{{ session()->get('email') }}"><input type="hidden" name="password" value="{{ session()->get('password') }}"><input type="hidden" name="force" value="{{ session()->get('force') }}"><div class="mupo-auth-field"><label>Verification Code</label><input type="password" name="two_step_verify_code" placeholder="{{ __('frontend.Verification Code') }}" required></div>@if($errors->first('email'))<div class="mupo-auth-alert error">{{ $errors->first('email') }}</div>@endif<button type="submit" class="mupo-auth-submit">{{ __('frontend.Verify My Account') }}</button></form><div class="mupo-auth-links">{{ __('frontend.Haven’t received it.') }} <a href="{{ route('resend_verification_code') }}">{{ __('frontend.Resend a new code.') }}</a></div></section></main></div></div>
@endsection
